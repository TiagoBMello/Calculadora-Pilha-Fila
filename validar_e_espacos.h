// validar_e_espacos.h
#pragma once

// Valida a expressão textual (aceita - unário como no seu código).
// Retorna 1 se for válida; 0 se inválida.
int validar_expressao(char expressao[]);

// Remove todos os espaços da string, in-place.
void remover_espacos(char expressao[]);
